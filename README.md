# Digital Supplement

- `paper_renv.lock`: R packages used to construct the plots in the publication
- `data_prep_env-1.pin.txt` and `data_prep_env-2.pin.txt`: Python packages 
  used for data preperation and anaylsis