# Digital Supplement

- `paper_renv.lock`: R packages used to construct the plots in the publication
- `data_prep_env-1.pin.txt` and `data_prep_env-2.pin.txt`: Python packages 
  used for data preperation and anaylsis
- `column_comparison.csv`: Results of the pairwise analysis of columns representing the same variables
- `missing_flux.csv`: Results of the missing data and flux analysis of each column
